from db.models import Slots


def get_slots_in_week_by_filter(date, filter):
    """Return list of slots for the day: <date>
    """
    # list_slots_in_week_by_filter = Slots.query.filter(Slots.start_interval.startswith(date)).all()
    # return list_slots_in_week_by_filter
    pass
